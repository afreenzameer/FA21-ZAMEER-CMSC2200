package edu.ben.lab2;

public class Conditional {
    public static void main(String[] args) {
        System.out.println(myConditional(3));
        System.out.println(mySwitch(3));
        System.out.println(myTernary(3));
    }

    private static String myConditional(int value) {
        if (value > 0) {
            return "Positive";
        } else if (value < 0) {
            return "Negative";
        } else {
            return "0";
        }

    }

    private static String mySwitch(int value) {
        switch (value) {
            case 1:
                return "One";

            case 2:
                return "Two";

            case 3:
                return "Three";

            default:
                return "Invalid";
        }
    }

    private static String myTernary(int value) {
        return value >= 0 ? "whole number" : "negative integer";
    }


}
